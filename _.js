_ = {
  clamp: function(inputNumber, lowerBound, upperBound){
    return Math.min(Math.max(inputNumber, lowerBound), upperBound);
  }
};

// Do not write or modify code below this line.
module.exports = _;